# keliriji-skills 项目宪章

> 本宪章是 `keliriji-skills` 的最高项目约束。新增或修改技能前，必须先检查是否符合本宪章；需要改变边界时，先修订宪章，再实施代码或文档。

## 1. 项目定位

`keliriji-skills` 是面向整个颗粒日记系统的 Agent Skills 权威仓库，不是 `keli-math-skills` 的改名副本，也不是某个客户端或后端服务的镜像。

它负责把颗粒日记已经存在的产品能力组织为 Agent 可理解、可路由、可安全调用的工作流，覆盖：

- 用户接入与 API Key 管理；
- 素材、知识颗粒与素材关系图；
- 学习流（LEC）、复习反馈与离屏复习；
- 品题（TTQ）与实战答题纸（PAS）；
- 颗粒工厂、学情与资源能力；
- `yuwen`、`math`、`english` 学科差异。

本仓库不复制 AppFunctor、Runtime、OSR、AppFactory 或学科服务中的业务实现。技能描述调用方法、工作流、边界和失败处理，业务真相仍由所属服务维护。

## 2. 继承与弃用

1. `keli-math-skills` 进入 deprecated 状态，只接受迁移所需的安全修复，不再承载新功能。
2. 数学能力迁入本仓库的 `math` 学科层，不再作为默认学科或独立总入口。
3. 可继承旧仓库的 Progressive Disclosure、意图路由、鉴权护栏和流程检查表，但不得继承其“数学即系统”的假设。
4. 新的安装、文档、Agent 集成和能力扩展统一指向 `keliriji-skills`。
5. 迁移期可识别旧环境变量或旧术语；新文档不得要求用户新建旧命名配置。

## 3. 三层能力模型

### L0：系统通用层

定义所有学科共享的身份、会话、隐私、素材、颗粒、学习流、反馈和错误处理规则。L0 不包含学科专属推理规则。

### L1：学科契约层

每个学科必须有独立契约，并使用稳定标识：

| 标识 | 学科 | 契约权威源 |
|------|------|------------|
| `yuwen` | 语文 | `appfunctor-yuwen` |
| `math` | 数学 | `appfunctor-math` |
| `english` | 英语 | `appfunctor-english` |

L1 只声明学科适用范围、术语、允许的流和差异化输入输出。学科理论的权威定义仍在对应学科仓库；本仓库只保留调用所需的最小摘要和来源链接。

### L2：任务工作流层

将用户意图路由为可执行流程，例如素材整理、开始学习流、提交反馈、PAS 练习或 OSR 对话。L2 必须组合 L0 通用规则与一个明确的 L1 学科上下文，不能靠猜测悄悄选择学科。

## 4. 设计原则

1. **用户目标优先于接口目录。** Agent 先识别用户要完成的学习任务，再加载对应 reference；不向用户倾倒全部 API。
2. **学科是一等上下文。** 涉及内容理解、学习流或练习时，必须从用户上下文取得或确认 `subject`；只有已有会话明确携带学科时才能继承。
3. **流程与真相分离。** 本仓库可以规定流程，不得伪造 endpoint、scope、枚举、响应字段或产品可用状态。
4. **渐进披露。** `skill/SKILL.md` 只负责触发、硬约束和路由；详细流程放入 `skill/references/`，设计与治理材料放入 `docs/`。
5. **可恢复闭环。** 创建会话、学习进度、反馈、备注等私有学习状态必须落到权威后端；Agent 不得用对话记忆或本地临时数据冒充持久化成功。
6. **端无关。** 技能表达颗粒日记业务语义，不绑定 Mobile2、小程序或 Web 的页面结构；需要展示时可返回 surface action，但不得把某一端 UI 当作业务真相。

## 5. 接口与执行边界

Agent 的颗粒日记业务调用统一经过 OpenAPI Skills Gateway：

```text
Agent -- API Key --> skills.keliriji.com -- delegated user session --> AppFunctor
```

强制规则：

1. 禁止 Agent 直连 `*.fcapp.run`、AppFunctor 内部路由、数据库、OSS 私有对象或服务令牌接口。
2. 禁止在 skill、示例、日志、对话可分享内容或 git 中写入 API Key、JWT、Refresh Token、服务令牌和签名 URL。
3. Gateway 未公开的能力标为 `planned`，不得提供看似可执行的请求示例。
4. 已公开能力必须以 Gateway 的公开 README、OpenAPI 契约或可验证路由为依据，并标记所需 scope。
5. 用户身份必须由委托会话确定；忽略或拒绝调用方试图覆盖的 `userId`、`learnerId`、`familyId` 等归属字段。

## 6. 能力成熟度

每项能力必须使用以下状态之一：

| 状态 | 含义 | Agent 行为 |
|------|------|------------|
| `available` | Gateway 已有公开且可验证的契约 | 可执行，仍需鉴权与输入确认 |
| `limited` | 仅部分动作或学科可用 | 明示限制后执行可用部分 |
| `planned` | 产品存在，但尚无公开 Skills 契约 | 解释当前边界，不伪造调用 |
| `deprecated` | 只为迁移保留 | 引导到替代能力 |

任何文档不得把客户端已有页面等同于 Skills API 已开放。

## 7. 隐私与安全

1. 用户上传素材、知识颗粒、学习记录、答题纸、LearnerSession、Shard 与 Topic 均按用户私有数据处理。
2. Skill 只请求完成当前任务所需的最小数据，不在结果中回显无关私有正文。
3. 诊断信息必须脱敏；不得记录令牌、完整 API Key、签名 URL、原始答题纸或完整会话快照。
4. 完整 API Key 仅在创建或轮换时显示一次；Agent 应引导用户存入平台密钥库或本地环境变量 `KELIRIJI_API_KEY`。
5. 高影响操作必须确认目标和范围，包括永久删除、覆盖学习状态、批量创建内容以及产生费用的工厂操作。

## 8. 学科扩展规则

新增学科必须同时满足：

1. 有稳定的 subject id 和明确的学科契约权威源；
2. 在 `skill/references/subjects.md` 登记适用工作流、流枚举和差异；
3. 不改变 L0 通用对象的语义；需要新对象时先更新本宪章或域模型；
4. 提供至少一条成功路由、一个歧义场景和一个不适用场景；
5. 不把一个学科的题型、评价方法或提示词推广为全系统默认。

## 9. 质量门槛

每次发布至少验证：

- `SKILL.md` frontmatter 可解析，名称稳定为 `keliriji`；
- 所有相对链接存在；
- 状态为 `available` 的 endpoint 与当前 Gateway 契约一致；
- 三个现有学科均能被路由，且未知学科不会静默回退到数学；
- 示例不包含 Secret、内部域名或 FC 地址；
- deprecated 能力提供明确的新入口。

## 10. 治理与权威顺序

发生冲突时按以下顺序处理：

1. 用户隐私、安全与数据所有权约束；
2. 本宪章；
3. 业务所属仓库的宪章或 constitution；
4. Gateway 的公开接口契约；
5. 本仓库 `skill/references/` 与 `docs/`。

若后两级与前一级冲突，停止执行并修正文档，不以“兼容旧技能”为理由绕过约束。

## 11. 当前实施范围

第一阶段交付：

- 通用 Skill 入口、鉴权、意图路由和术语表；
- 素材与知识颗粒、LEC 复习流程；
- PAS、TTQ、OSR 等能力的边界说明；
- 三学科契约与扩展方式；
- 能力矩阵、迁移说明、路线图和自动结构校验。

第一阶段不承诺新增后端 endpoint。发现缺口时在路线图中登记，由 Gateway 或业务所属服务实现并发布契约后，再将能力状态提升为 `available`。
## 12. 第一方 LEC 对话委托（2026-09-07）

仅 `/api/v1/open/review/coach/evidence` 与 `/coach/interventions` 允许第一方 keliagent 使用当前用户登录 JWT 委托。Gateway 必须向 AppFunctor 验证 JWT 并保持 self，后端每次核验 keliflo 学习者绑定与 AppFactory 已付款学科。不得接受客户端自报归属。第三方 API Key 仍分别要求 read / review；其它 OpenAPI 路由不接受此例外。Agent 业务访问仍只能走 Gateway。
